define([
  'backbone'
], function(Backbone) {
  return Backbone.Model.extend({
      // TODO: set up initial model structure

      finish: function(outputModel) {
        //outputModel.set('security_groups', [this.toJSON()]);
      }
  });
});
